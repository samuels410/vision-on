<div class="slider">
  <div class="container-fluid">
    <div id="heroSlider" class="carousel slide">
      <div class="carousel-inner" style="margin-top: -62px;">
        <% @album.photos.each_with_index do |photo , i|%>
             <% if i == 0 %>
                <div class="active item">
                  <% else  %>
            <div class="item">
              <%end  %>

          <div class="hero-unit">
            <div class="row-fluid">
              <div class="span4 marketting-info">
                <h3><%= photo.title %></h3>
                <p style="margin-top: 46px">
                  <%= photo.description %>
                </p>
              </div>
              <div class="span8 thumbnail">
                <%= image_tag(photo.file.url,:style => "height: 526px;width: 100%;") %>
              </div>
            </div>
          </div>
        </div>

        <%end%>
        </div>
      </div>
      <a class="left carousel-control" href="#heroSlider" data-slide="prev">‹</a>
      <a class="right carousel-control" href="#heroSlider" data-slide="next">›</a>
    </div>
  </div>
</div>